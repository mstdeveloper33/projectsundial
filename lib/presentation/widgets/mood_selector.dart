/*import 'package:flutter/material.dart';
import 'package:sundialproject/presentation/screens/journaling/viewmodel/journal_viewmodel.dart';

class MoodSelector extends StatelessWidget {
  final Function(String) onMoodSelected;

  const MoodSelector({super.key, required this.onMoodSelected, required JournalingViewModel viewModel});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        IconButton(
          icon: const Text("😊"), // Emoji for happy mood
          onPressed: () => onMoodSelected("happy"),
        ),
        IconButton(
          icon: const Text("😞"), // Emoji for sad mood
          onPressed: () => onMoodSelected("sad"),
        ),
        IconButton(
          icon: const Text("😎"), // Emoji for cool mood
          onPressed: () => onMoodSelected("cool"),
        ),
      ],
    );
  }
}
*/
